[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --no-fund --no-audit"]

[phases.build]
cmds = ["npm run build -w @workspace/api-server"]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"

